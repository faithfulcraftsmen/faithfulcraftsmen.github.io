# build output
dist/
.cache
# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# Astro
.astro/

# Claude Code local settings (logs and machine-local overrides only)
.claude/logs/
.claude/settings.local.json

# Photo intake drop zone — raw photos stay on disk, never committed.
# Only the workflow README is tracked; optimized output lands in public/.
intake/*
!intake/README.md
